{
  "name": "@copycoder/core",
  "version": "0.1.0",
  "description": "Core logic for Copy Coder",
  "main": "dist/index.js", // Entry point after build
  "types": "dist/index.d.ts", // Type definitions entry point
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["copy", "code", "clipboard", "core"],
  "author": "Your Name <your.email@example.com>", // TODO: Update author info
  "license": "MIT", // Or your chosen license
  "dependencies": {
     "ignore": "^5.3.0" // Example dependency
  },
  "devDependencies": {
    // Core-specific dev dependencies if needed
  },
  "publishConfig": {
    "access": "public" // Or "restricted" if private npm package
  }
}