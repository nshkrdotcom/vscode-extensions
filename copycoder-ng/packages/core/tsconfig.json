{
  "extends": "../../tsconfig.base.json", // Inherit from the base config
  "compilerOptions": {
    "outDir": "./dist", // Output directory specific to this package
    "rootDir": "./src", // Source directory specific to this package
    "composite": true // Enable project references if needed later
  },
  "include": ["src/**/*"], // Only include files in the src directory
  "exclude": ["node_modules", "dist", "**/*.test.ts"] // Exclude tests if they exist
}